import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react( )],
  base: '/curso-pln-ia/', // Certifique-se de que este é o nome do seu repositório no GitHub
})
